(function(){
function init(){
  var chapter=document.querySelector('.gallery-chapter'),section=chapter&&chapter.querySelector('.motion-gallery'),viewport=section&&section.querySelector('.motion-gallery-viewport'),track=section&&section.querySelector('.motion-gallery-track'),signal=chapter&&chapter.querySelector('.gallery-signal');if(!chapter||!section||!viewport||!track)return;
  var reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,raf=0,lastT=performance.now(),lastY=scrollY,direction=1,targetDirection=1,ambient=.055,currentSpeed=.055,targetSpeed=.055,boost=0,signalCurrent=0,signalTarget=0,revealCurrent=0,revealTarget=0,greenCurrent=0,greenTarget=0,loopWidth=0,normalizing=false,dragging=false,lastDragX=0,lastNativeScroll=0,buttonUntil=0;
  function clamp(v,a,b){return Math.max(a,Math.min(b,v))}function smooth(v){v=clamp(v,0,1);return v*v*(3-2*v)}function visible(){var r=chapter.getBoundingClientRect();return r.bottom>0&&r.top<innerHeight}
  function measureLoop(){var next=track.scrollWidth/3;if(!next)return;var first=!loopWidth;loopWidth=next;if(first){normalizing=true;viewport.scrollLeft=loopWidth;lastNativeScroll=loopWidth;requestAnimationFrame(function(){normalizing=false})}}
  function normalizeLoop(){if(!loopWidth||normalizing)return;var s=viewport.scrollLeft,shift=0;if(s<loopWidth*.62)shift=loopWidth;else if(s>loopWidth*1.38)shift=-loopWidth;if(shift){normalizing=true;viewport.scrollLeft=s+shift;lastNativeScroll=viewport.scrollLeft;requestAnimationFrame(function(){normalizing=false})}}
  function readProgress(){var r=chapter.getBoundingClientRect(),vh=innerHeight,p=clamp((vh*.94-r.top)/(vh*.72),0,1);revealTarget=smooth(clamp(p/.52,0,1));greenTarget=smooth(clamp((p-.22)/.58,0,1));signalTarget=smooth(clamp((p-.40)/.48,0,1))}
  function paint(){chapter.style.setProperty('--gallery-reveal',revealCurrent.toFixed(4));chapter.style.setProperty('--green-rise',greenCurrent.toFixed(4));if(signal){signal.style.setProperty('--signal-p',signalCurrent.toFixed(4));chapter.classList.toggle('signal-on-green',greenCurrent>.64)}}
  function frame(now){raf=0;var dt=Math.min(34,Math.max(1,now-lastT));lastT=now,ease=1-Math.exp(-dt/120);revealCurrent+=(revealTarget-revealCurrent)*ease;greenCurrent+=(greenTarget-greenCurrent)*ease;signalCurrent+=(signalTarget-signalCurrent)*ease;direction+=(targetDirection-direction)*(1-Math.exp(-dt/150));targetSpeed=ambient+boost;currentSpeed+=(targetSpeed-currentSpeed)*(1-Math.exp(-dt/180));boost*=Math.exp(-dt/240);if(!reduced&&visible()&&!dragging&&now>buttonUntil){viewport.scrollLeft+=currentSpeed*dt*direction;lastNativeScroll=viewport.scrollLeft;normalizeLoop()}paint();if(visible()&&!reduced)raf=requestAnimationFrame(frame)}
  function wake(){if(!raf&&!reduced){lastT=performance.now();raf=requestAnimationFrame(frame)}}
  function setDirection(dir,extra){targetDirection=dir;boost=Math.max(boost,extra||0);wake()}
  function onPageScroll(){var y=scrollY,dy=y-lastY;lastY=y;readProgress();if(Math.abs(dy)>.7)setDirection(dy>0?1:-1,Math.min(.15,Math.abs(dy)*.0014));wake()}
  function step(dir){setDirection(dir,.10);buttonUntil=performance.now()+430;viewport.scrollBy({left:dir*Math.min(innerWidth*.44,500),behavior:'smooth'});setTimeout(function(){normalizeLoop();buttonUntil=0;wake()},450)}
  chapter.addEventListener('click',function(e){var btn=e.target.closest('[data-gallery-step]');if(!btn)return;e.preventDefault();e.stopPropagation();step(btn.getAttribute('data-gallery-step')==='prev'?-1:1)});
  viewport.addEventListener('pointerdown',function(e){dragging=true;lastDragX=e.clientX;boost=0},{passive:true});viewport.addEventListener('pointermove',function(e){if(!dragging)return;var dx=e.clientX-lastDragX;if(Math.abs(dx)>1)setDirection(dx<0?1:-1,Math.min(.13,Math.abs(dx)*.004));lastDragX=e.clientX},{passive:true});function release(){dragging=false;normalizeLoop();wake()}viewport.addEventListener('pointerup',release,{passive:true});viewport.addEventListener('pointercancel',release,{passive:true});viewport.addEventListener('touchend',release,{passive:true});
  viewport.addEventListener('scroll',function(){if(normalizing)return;var s=viewport.scrollLeft,delta=s-lastNativeScroll;if(dragging&&Math.abs(delta)>.5)targetDirection=delta>0?1:-1;lastNativeScroll=s;normalizeLoop()},{passive:true});viewport.addEventListener('wheel',function(e){if(Math.abs(e.deltaX)>Math.abs(e.deltaY)*.65)setDirection(e.deltaX>=0?1:-1,Math.min(.14,Math.abs(e.deltaX)*.0014))},{passive:true});
  measureLoop();readProgress();revealCurrent=revealTarget;greenCurrent=greenTarget;signalCurrent=signalTarget;paint();window.addEventListener('scroll',onPageScroll,{passive:true});window.addEventListener('resize',function(){measureLoop();readProgress();wake()},{passive:true});if('ResizeObserver'in window)new ResizeObserver(measureLoop).observe(track);if('IntersectionObserver'in window)new IntersectionObserver(function(entries){if(entries[0].isIntersecting){measureLoop();readProgress();wake()}},{threshold:.01}).observe(chapter);wake();
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();