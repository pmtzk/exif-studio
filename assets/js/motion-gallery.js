(function(){
function init(){
  var chapter=document.querySelector('.gallery-chapter'),section=chapter&&chapter.querySelector('.motion-gallery'),viewport=section&&section.querySelector('.motion-gallery-viewport'),track=section&&section.querySelector('.motion-gallery-track'),signal=chapter&&chapter.querySelector('.gallery-signal');if(!chapter||!section||!viewport||!track)return;
  var reduced=matchMedia('(prefers-reduced-motion: reduce)').matches,raf=0,lastT=performance.now(),loopWidth=0,isVisible=true,progressDirty=true,lastScrollY=window.scrollY;
  var signalCurrent=0,signalTarget=0,revealCurrent=0,revealTarget=0,greenCurrent=0,greenTarget=0;
  var SPEED=.044,position=0,velocity=SPEED,direction=1,inputVelocity=0,inputTarget=0,inputActive=false,buttonBoost=0;
  var pointerId=null,pointerKind='',axis=null,startX=0,startY=0,lastX=0,lastPointerT=0;
  var wheelSession=false,wheelTimer=0,wheelVelocity=0;
  var INPUT_SCALE=.85;
  function clamp(v,a,b){return Math.max(a,Math.min(b,v))}function smooth(v){v=clamp(v,0,1);return v*v*(3-2*v)}
  function measureLoop(){var c=track.children;if(c.length<2)return;var a=c[0].getBoundingClientRect(),b=c[Math.floor(c.length/3)].getBoundingClientRect(),w=b.left-a.left;if(w>0)loopWidth=w}
  function render(){if(!loopWidth)return;var x=((position%loopWidth)+loopWidth)%loopWidth;track.style.transform='translate3d('+(-x-loopWidth)+'px,0,0)'}
  function readProgress(){var r=chapter.getBoundingClientRect(),vh=innerHeight,p=clamp((vh*1.20-r.top)/(vh*.94),0,1);revealTarget=smooth(clamp(p/.52,0,1));greenTarget=smooth(clamp((p-.22)/.58,0,1));var signalStart=vh*.72;var sp=clamp((signalStart-r.top)/(vh*.62),0,1);signalTarget=smooth(sp);progressDirty=false}
  function signalTouchesPhotos(){if(!signal)return false;var first=track.querySelector('.motion-gallery-item');if(!first)return false;var sr=signal.getBoundingClientRect(),pr=first.getBoundingClientRect();return sr.bottom>=pr.top}
  function paintProgress(){chapter.style.setProperty('--gallery-reveal',revealCurrent.toFixed(4));chapter.style.setProperty('--green-rise',greenCurrent.toFixed(4));if(signal){signal.style.setProperty('--signal-p',signalCurrent.toFixed(4));chapter.classList.toggle('signal-on-green',signalTouchesPhotos())}}
  function frame(now){raf=0;var dt=Math.min(34,Math.max(1,now-lastT));lastT=now;if(progressDirty)readProgress();var pe=1-Math.exp(-dt/145);revealCurrent+=(revealTarget-revealCurrent)*pe;greenCurrent+=(greenTarget-greenCurrent)*pe;signalCurrent+=(signalTarget-signalCurrent)*pe;if(!reduced&&isVisible){var cruise=SPEED*direction;if(inputActive){inputVelocity+=(inputTarget-inputVelocity)*(1-Math.exp(-dt/22));velocity=inputVelocity}else{var desired=cruise+buttonBoost;velocity+=(desired-velocity)*(1-Math.exp(-dt/180));buttonBoost*=Math.exp(-dt/210);if(Math.abs(buttonBoost)<.001)buttonBoost=0}position+=velocity*dt;render()}paintProgress();var pm=Math.abs(revealTarget-revealCurrent)>.001||Math.abs(greenTarget-revealCurrent)>.001||Math.abs(signalTarget-signalCurrent)>.001;if((isVisible&&!reduced)||pm)raf=requestAnimationFrame(frame)}
  function wake(){if(!raf){lastT=performance.now();raf=requestAnimationFrame(frame)}}
  function setImpulse(v){if(Math.abs(v)<.001)return;direction=v>0?1:-1;inputTarget=clamp(v*INPUT_SCALE,-1.15,1.15);inputActive=true;wake()}
  function releaseInput(){if(!inputActive)return;inputActive=false;inputTarget=0;wake()}
  function step(dir){direction=dir;inputActive=false;buttonBoost=dir*.064;wake()}
  chapter.addEventListener('click',function(e){var b=e.target.closest('[data-gallery-step]');if(!b)return;e.preventDefault();e.stopPropagation();step(b.getAttribute('data-gallery-step')==='prev'?-1:1)});
  function startPointer(id,kind,x,y){pointerId=id;pointerKind=kind||'mouse';axis=null;startX=lastX=x;startY=y;lastPointerT=performance.now()}
  function movePointer(id,x,y,prevent){if(id!==pointerId)return;var tx=x-startX,ty=y-startY,ax=Math.abs(tx),ay=Math.abs(ty);if(!axis&&(ax>3||ay>3)){if(ax>ay*1.08){axis='x';viewport.classList.add('is-horizontal-drag')}else if(ay>ax*1.45){axis='y'}}if(axis!=='x')return;if(prevent)prevent();var now=performance.now(),dt=Math.max(8,now-lastPointerT),dx=x-lastX;setImpulse(clamp((-dx/dt),-1.35,1.35));lastX=x;lastPointerT=now}
  function finishPointer(id){if(pointerId===null||id!==pointerId)return;var horizontal=axis==='x';viewport.classList.remove('is-horizontal-drag');pointerId=null;pointerKind='';axis=null;if(horizontal)releaseInput();wake()}
  if(window.PointerEvent){viewport.addEventListener('pointerdown',function(e){if(e.pointerType==='mouse'&&e.button!==0)return;startPointer(e.pointerId,e.pointerType,e.clientX,e.clientY);try{viewport.setPointerCapture(e.pointerId)}catch(_){}},{passive:true});viewport.addEventListener('pointermove',function(e){movePointer(e.pointerId,e.clientX,e.clientY,function(){if(e.cancelable)e.preventDefault()})},{passive:false});viewport.addEventListener('pointerup',function(e){finishPointer(e.pointerId);try{if(viewport.hasPointerCapture(e.pointerId))viewport.releasePointerCapture(e.pointerId)}catch(_){}},{passive:true});viewport.addEventListener('pointercancel',function(e){finishPointer(e.pointerId)},{passive:true})}else{viewport.addEventListener('touchstart',function(e){if(pointerId!==null||!e.touches.length)return;var t=e.touches[0];startPointer(t.identifier,'touch',t.clientX,t.clientY)},{passive:true});viewport.addEventListener('touchmove',function(e){if(pointerId===null)return;var t=null;for(var i=0;i<e.touches.length;i++)if(e.touches[i].identifier===pointerId){t=e.touches[i];break}if(t)movePointer(pointerId,t.clientX,t.clientY,function(){if(e.cancelable)e.preventDefault()})},{passive:false});viewport.addEventListener('touchend',function(){if(pointerId!==null)finishPointer(pointerId)},{passive:true});viewport.addEventListener('touchcancel',function(){if(pointerId!==null)finishPointer(pointerId)},{passive:true})}
  viewport.addEventListener('wheel',function(e){if(pointerId!==null)return;var ax=Math.abs(e.deltaX),ay=Math.abs(e.deltaY);if(!wheelSession){if(ax<.45||ax<=ay*1.05)return;wheelSession=true}if(e.cancelable)e.preventDefault();if(Math.abs(e.deltaX)>.05){wheelVelocity=e.deltaX*.0054;setImpulse(wheelVelocity)}clearTimeout(wheelTimer);wheelTimer=setTimeout(function(){wheelSession=false;wheelVelocity=0;releaseInput()},125);wake()},{passive:false});
  function onScroll(){var y=window.scrollY,dy=y-lastScrollY;lastScrollY=y;progressDirty=true;if(pointerId===null&&!wheelSession&&Math.abs(dy)>1.5){direction=dy>0?1:-1;if(!inputActive)velocity+=(SPEED*direction-velocity)*(1-Math.exp(-Math.min(Math.abs(dy),30)/10))}wake()}
  function stabilize(){var imgs=track.querySelectorAll('img'),jobs=[];imgs.forEach(function(img){if(img.decode)jobs.push(img.decode().catch(function(){}));else if(!img.complete)jobs.push(new Promise(function(res){img.addEventListener('load',res,{once:true});img.addEventListener('error',res,{once:true})}))});Promise.all(jobs).then(function(){requestAnimationFrame(function(){measureLoop();render();wake()})})}
  measureLoop();render();readProgress();revealCurrent=revealTarget;greenCurrent=greenTarget;signalCurrent=signalTarget;paintProgress();stabilize();window.addEventListener('scroll',onScroll,{passive:true});window.addEventListener('resize',function(){measureLoop();progressDirty=true;render();wake()},{passive:true});if('IntersectionObserver'in window)new IntersectionObserver(function(entries){isVisible=entries[0].isIntersecting;if(isVisible){progressDirty=true;render();wake()}},{threshold:.01}).observe(chapter);wake();
  if(!document.querySelector('script[data-approach-cards]')){var approach=document.createElement('script');approach.src='assets/js/approach-cards.js';approach.defer=true;approach.dataset.approachCards='';document.body.appendChild(approach)}
}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init,{once:true});else init();
})();