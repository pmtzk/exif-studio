(function(){
  function init(){
    var gallery=document.querySelector('.gallery-chapter');
    if(!gallery||document.querySelector('.exif-approach'))return;
    var section=document.createElement('section');
    section.className='exif-approach';section.id='approach';
    var summary='You already know what makes your place special. The question is whether someone who has never been there can see it. Some things are obvious when you’re there. The way the morning starts. The person everyone remembers. The corner people always end up in. Somewhere between the place itself and the screen, those things can get lost in translation.';
    var words=summary.split(' ').map(function(word,i){var x=((i*37)%41)-20,y=18+((i*29)%35),r=(((i*17)%9)-4)*.18,d=(i%7)*.018;return '<span class="approach-word" style="--wx:'+x+'px;--wy:'+y+'px;--wr:'+r+'deg;--wd:'+d+'">'+word+'</span>'}).join(' ');
    section.innerHTML='<div class="wrap"><div class="approach-layout"><div class="approach-deck" role="list"><button class="approach-card" type="button" data-approach="observe" aria-expanded="false" role="listitem"><span class="approach-card-inner"><span class="approach-card-title">Observe</span><span class="approach-card-copy"><span class="approach-card-rule"></span><strong>Start with what is already there.</strong><p>The things guests come back talking about. The rituals, details, people and moments that make the place feel like itself.</p></span><span class="approach-card-arrow">→</span></span></button><button class="approach-card" type="button" data-approach="decide" aria-expanded="false" role="listitem"><span class="approach-card-inner"><span class="approach-card-title">Decide</span><span class="approach-card-copy"><span class="approach-card-rule"></span><strong>Choose what deserves to become recognizable.</strong><p>Every property has more to say than it can show at once. Together, we decide what should lead.</p></span><span class="approach-card-arrow">→</span></span></button><button class="approach-card" type="button" data-approach="create" aria-expanded="false" role="listitem"><span class="approach-card-inner"><span class="approach-card-title">Create</span><span class="approach-card-copy"><span class="approach-card-rule"></span><strong>Make it unmistakable.</strong><p>We build from that decision, giving the property a visual world that feels specific to the experience behind it.</p></span><span class="approach-card-arrow">→</span></span></button></div><p class="approach-summary">'+words+'</p></div></div>';
    gallery.insertAdjacentElement('afterend',section);
    var cards=[].slice.call(section.querySelectorAll('.approach-card')),reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;
    var target=0,current=0,raf=0,interactive=false,activeIndex=0,wasAssembled=false;
    function clamp(v,a,b){return Math.max(a,Math.min(b,v))}function smooth(v){v=clamp(v,0,1);return v*v*(3-2*v)}
    function setActive(index){activeIndex=index;cards.forEach(function(c,i){var on=i===index;c.classList.toggle('is-active',on);c.setAttribute('aria-expanded',on?'true':'false')})}
    function activate(card){if(interactive)setActive(cards.indexOf(card))}
    function read(){var r=section.getBoundingClientRect(),vh=innerHeight;target=clamp((vh*.88-r.top)/(vh*.48),0,1);if(reduced)target=1}
    function paint(p){var open=smooth(clamp((p-.12)/.58,0,1)),copy=smooth(clamp((p-.24)/.64,0,1)),assembled=p>.94;section.style.setProperty('--approach-open',open.toFixed(4));section.style.setProperty('--approach-copy',copy.toFixed(4));section.style.setProperty('--active-index',activeIndex);interactive=assembled;section.classList.toggle('is-assembled',assembled);if(assembled&&!wasAssembled)setActive(activeIndex);if(!assembled){cards.forEach(function(c,i){c.classList.toggle('is-scroll-active',i===activeIndex);c.classList.remove('is-active');c.setAttribute('aria-expanded','false')})}if(p<.025){activeIndex=0;section.style.setProperty('--active-index','0')}wasAssembled=assembled}
    function frame(){raf=0;var d=target-current;current+=d*.13;if(Math.abs(d)<.001)current=target;paint(current);if(Math.abs(target-current)>.001)raf=requestAnimationFrame(frame)}
    function wake(){read();if(!raf)raf=requestAnimationFrame(frame)}
    cards.forEach(function(card){card.addEventListener('click',function(){activate(card)});card.addEventListener('mouseenter',function(){if(matchMedia('(hover:hover) and (pointer:fine)').matches)activate(card)});card.addEventListener('focus',function(){activate(card)})});
    addEventListener('scroll',wake,{passive:true});addEventListener('resize',wake,{passive:true});read();current=reduced?1:target;paint(current);wake();
  }
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',function(){setTimeout(init,0)},{once:true});else setTimeout(init,0);
})();